import { withFooterLayout } from '../wrappers/WithFooterLayout';

export const Profile = withFooterLayout(() => {
  return (
    <div>
      <h1>Profile</h1>
      <div style={{ height: '100%' }}>
        <p>kasjkasdjd</p>
      </div>
    </div>
  );
});
